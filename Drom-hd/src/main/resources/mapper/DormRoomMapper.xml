<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dormitory.mapper.DormRoomMapper">

    <update id="increaseOccupancy">
        UPDATE dorm_room
        SET occupied_beds = occupied_beds + 1
        WHERE room_id = #{roomId}
          AND occupied_beds &lt; room_capacity </update>

    <update id="decreaseOccupancy">
        UPDATE dorm_room
        SET occupied_beds = occupied_beds - 1
        WHERE room_id = #{roomId}
          AND occupied_beds &gt; 0 </update>

</mapper>